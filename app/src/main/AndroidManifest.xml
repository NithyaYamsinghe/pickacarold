<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pickacar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Reviews.UserReviews"></activity>
        <activity android:name=".Reviews.AddReview" />
        <activity android:name=".Reviews.ReviewsList" />
        <activity android:name=".User.RegisterActivity" />
        <activity android:name=".User.LoginActivity" />
        <activity android:name=".User.OnBoardingActivity" />
        <activity android:name=".Cars.SelectCar" />
        <activity android:name=".Cars.CarDescription" />
        <activity android:name=".User.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>